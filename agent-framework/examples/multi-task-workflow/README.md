# Multi-Task Workflow Example

Example showing task orchestration and parallel execution.

## Usage

```bash
cd agent-framework/examples/multi-task-workflow
pnpm install && pnpm build
ANTHROPIC_API_KEY=your_key node dist/index.js
```

See full implementation in source code.
